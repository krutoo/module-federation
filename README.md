# Module Federation Example

Example of storing module remote entry url in page (not in bundle)

## Run

1. In terminal:

```bash
# terminal 1
cd app-child && npm i && npm run dev

# terminal 2
cd app-parent && npm i && npm run dev
```

2. In browser open http://localhost:8500
